services:
  ingestor:
    build:
      context: .
      dockerfile: ingestor/Dockerfile
    env_file:
      - .env

  dispatcher:
    build:
      context: .
      dockerfile: dispatcher/Dockerfile
    env_file:
      - .env